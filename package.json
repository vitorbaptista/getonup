{
  "name": "getonup-monorepo",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "getonup — open-source, self-hostable tool to turn any AI-generated web artifact into a live, shareable URL.",
  "workspaces": [
    "cli",
    "server"
  ],
  "scripts": {
    "setup": "npm run build && (npm link --workspace cli || true) && node scripts/local-setup.mjs",
    "predev": "node scripts/local-setup.mjs --quiet",
    "dev": "npm --workspace server run dev",
    "deploy": "npm --workspace server run deploy",
    "getonup": "npm --workspace cli run dev --",
    "build": "npm --workspace cli run build",
    "test": "npm --workspace cli run test",
    "typecheck": "npm --workspace cli run typecheck && npm --workspace server run typecheck",
    "demo": "node scripts/publish-demo.mjs"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22.18"
  }
}
