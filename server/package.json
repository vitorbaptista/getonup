{
  "name": "conjure-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Conjure server — a single Cloudflare Worker that stores deploys in R2 and serves them at /s/:id.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.20.0"
  }
}
